[user]
	name = royisy
	email = 28505196+royisy@users.noreply.github.com
[pager]
	branch = false
[core]
	editor = vi
	pager = hunk pager
[interactive]
	# hunk has no --color-only filter, so `git add -p` still goes through delta.
	diffFilter = delta --color-only
[merge]
	conflictStyle = zdiff3
[push]
	autoSetupRemote = true
[include]
	path = ~/.gitconfig.local
